## Experiment 1 — keep

**Score:** 25/25 (100%)  
**Change:** Added server action auth pattern, RLS boilerplate with `ENABLE ROW LEVEL SECURITY`, named index conventions, no-`SELECT *` rule, and all project table names.  
**Reasoning:** Skill was a blank category index — zero actual rules. Adding real content covering all 5 evals was the obvious first move.  
**Result:** All 5 evals passed across all 5 test inputs. 0% → 100% in one mutation.  
**Failing outputs:** None. Hit ceiling on first experiment.
